@using BoardBrawl.WebApp.MVC.Areas.Game.Models;
@model Model

<div class="h-100 d-flex flex-column justify-content-between text-white">
    <div class="mt-3">
        @await Html.PartialAsync("GamePanel/_CardSearch")
    </div>

    <div class="flex-grow-1 overflow-auto mt-3">
        @await Html.PartialAsync("GamePanel/_CardSearchHistory")
    </div>
</div>